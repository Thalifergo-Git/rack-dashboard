# The header strip. `resources` here describes the DASHBOARD host; per-host
# detail lives in the Performance group in services.yaml.
- resources:
    cpu: true
    memory: true
    disk: /
- datetime:
    text_size: xl
    format:
      timeStyle: short
